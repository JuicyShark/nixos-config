# Keep nix-direnv profiles local to each machine. This checkout is shared over
# NFS, so a project-local .direnv would mix Linux and Darwin store paths.
direnv_layout_dir() {
  printf '%s\n' "${XDG_CACHE_HOME:-$HOME/.cache}/direnv/layouts${PWD}"
}

use flake
